# Towazon-landing

- Landing page collects early-access requests and posts them to SheetDB. Set your SheetDB endpoint in `index.html` and `assets/js/main.js` by replacing `YOUR_SHEETDB_ID` with your API ID.
